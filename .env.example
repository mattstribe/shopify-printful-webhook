# Base URL for design art (and number/placement art). No trailing slash.
# Example: your R2 bucket public URL (e.g. https://pub-xxxx.r2.dev for variant-apparel)
# Art is looked up under: ART_BASE_URL/{designId}/{filename}.png
ART_BASE_URL=

# Shopify
SHOPIFY_STORE_DOMAIN=
SHOPIFY_ADMIN_TOKEN=

# Printful
PRINTFUL_API_TOKEN=

# Composite upload: API that receives the composite PNG and public URL for the uploaded file.
# COMPOSITE_PUBLIC_BASE_URL = public base where composites are served (e.g. same R2 bucket URL as ART_BASE_URL, or different). No trailing slash. If unset, ART_BASE_URL is used.
# COMPOSITE_UPLOAD_API_URL=https://stribe-api.vercel.app/api/upload
COMPOSITE_PUBLIC_BASE_URL=
# COMPOSITE_UPLOAD_PLUGIN_ID=variant-merch

# Optional: custom number field keys (comma-separated, for jersey number extraction)
# CUSTOM_NUMBER_FIELD_KEYS=

# Optional: debug
# DEBUG_TOKEN=
